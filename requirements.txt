# Agent loop — provider-pluggable. OpenAI is the default backend (you have a key);
# Anthropic is a drop-in alternative. Install whichever you use.
openai>=1.40.0
python-dotenv>=1.0.0           # load OPENAI_API_KEY from a local .env
# anthropic>=0.40.0    # uncomment if/when you have an Anthropic key

# RAG: local embeddings + vector store (offline, vendor-independent)
sentence-transformers>=2.2.0   # all-MiniLM-L6-v2, runs locally
chromadb>=0.4.0

# Test suite against Toolshop API
pytest>=7.0.0
pytest-randomly>=3.12.0        # deterministic-but-genuine order-dependent flakiness
requests>=2.28.0

# Contract checking
jsonschema>=4.0.0              # validate responses against OpenAPI component schemas
PyYAML>=6.0                    # load the OpenAPI spec

# CLI trace formatting
rich>=13.0.0
